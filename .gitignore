# ── SECRETS — never commit these ──
config.php
.env
*.env

# ── LOGS ──
*.log
logs/

# ── OS JUNK ──
.DS_Store
Thumbs.db
desktop.ini

# ── EDITOR FILES ──
.vscode/
.idea/

# ── INTERNAL TOOLS — Pi only, never deploy ──
internal/
